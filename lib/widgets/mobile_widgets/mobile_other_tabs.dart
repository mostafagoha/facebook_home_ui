import 'package:flutter/material.dart';

class OtherTabs extends StatelessWidget {
  const OtherTabs({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tabs'),
    );
  }
}
